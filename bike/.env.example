# ridegoa — MySQL connection (copy this file to `.env` and fill in)
# Used by src/lib/db.ts (server-side only)
RIDEGOA_DB_HOST=localhost
RIDEGOA_DB_PORT=3306
RIDEGOA_DB_USER=root
RIDEGOA_DB_PASSWORD=your_mysql_password
RIDEGOA_DB_NAME=ridegoa
